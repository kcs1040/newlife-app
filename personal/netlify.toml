[build]
  functions = "netlify/functions"
  publish = "."

# 한 파일짜리 앱이라 캐시할 이유가 없다. 브라우저가 옛 화면을 쥐고 있으면
# 데이터만 새로 오고 레이아웃은 그대로여서 무엇이 고쳐졌는지 알 수 없다.
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, must-revalidate"
